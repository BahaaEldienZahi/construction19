<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_boq_variation_order_form" model="ir.ui.view">
        <field name="name">boq.variation.order.form</field>
        <field name="model">boq.variation.order</field>
        <field name="arch" type="xml">
            <form string="Variation Order">
                <header>
                    <button name="action_submit_for_approval" string="Submit for Approval" type="object"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve_l1" string="Approve (L1)" type="object"
                            class="btn-primary" invisible="state != 'submitted'"
                    />
                    <button name="action_approve_l2" string="Final Approve (L2)" type="object"
                            class="btn-success" invisible="state != 'approved_l1'"
                    />
                    <button name="action_reject" string="Reject" type="object"
                            invisible="state not in ('submitted', 'approved_l1')"/>
                    <button name="action_reset_draft" string="Reset to Draft" type="object"
                            invisible="state != 'rejected'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved_l1,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="project_id" readonly="state != 'draft'"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="total_delta_amount"/>
                            <field name="applied" invisible="not applied"/>
                        </group>
                    </group>
                    <group string="Reason">
                        <field name="reason" nolabel="1" placeholder="سبب أمر التغيير..."/>
                    </group>
                    <field name="line_ids" readonly="state != 'draft'">
                        <list editable="bottom">
                            <field name="boq_line_id"/>
                            <field name="parent_boq_line_id"/>
                            <field name="code"/>
                            <field name="name"/>
                            <field name="uom_id"/>
                            <field name="delta_qty"/>
                            <field name="unit_price"/>
                            <field name="delta_amount" sum="Total"/>
                        </list>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_boq_variation_order_list" model="ir.ui.view">
        <field name="name">boq.variation.order.list</field>
        <field name="model">boq.variation.order</field>
        <field name="arch" type="xml">
            <list string="Variation Orders" decoration-success="state == 'approved'"
                  decoration-danger="state == 'rejected'">
                <field name="name"/>
                <field name="project_id"/>
                <field name="date"/>
                <field name="total_delta_amount" sum="Total"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"
                       decoration-info="state in ('submitted', 'approved_l1')"/>
            </list>
        </field>
    </record>

    <record id="view_boq_variation_order_search" model="ir.ui.view">
        <field name="name">boq.variation.order.search</field>
        <field name="model">boq.variation.order</field>
        <field name="arch" type="xml">
            <search string="Search Variation Orders">
                <field name="name"/>
                <field name="project_id"/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Pending Approval" name="pending" domain="[('state', 'in', ('submitted', 'approved_l1'))]"/>
                <filter string="Approved" name="approved" domain="[('state', '=', 'approved')]"/>
                <group>
                    <filter string="Project"
                            name="group_project"
                            context="{'group_by': 'project_id'}"/>

                    <filter string="Status"
                            name="group_state"
                            context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_boq_variation_order" model="ir.actions.act_window">
        <field name="name">Variation Orders</field>
        <field name="res_model">boq.variation.order</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_boq_variation_order" name="Variation Orders"
              parent="menu_commercial"
              action="action_boq_variation_order" sequence="10"/>

</odoo>